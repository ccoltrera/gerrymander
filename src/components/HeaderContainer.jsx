'use strict';
var React = require('react');

var HeaderContainer = React.createClass({
  render: function() {
    return (
      <header>
        <h1>Gerrymander</h1>
      </header>
    );
  }
});

module.exports = HeaderContainer;
